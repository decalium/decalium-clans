--- a/net/minecraft/world/entity/ai/goal/RunAroundLikeCrazyGoal.java
+++ b/net/minecraft/world/entity/ai/goal/RunAroundLikeCrazyGoal.java
@@ -8,6 +8,7 @@
 import net.minecraft.world.phys.Vec3;
 
 public class RunAroundLikeCrazyGoal extends Goal {
+
     private final AbstractHorse horse;
     private final double speedModifier;
     private double posX;
@@ -20,15 +21,17 @@
         this.setFlags(EnumSet.of(Goal.Flag.MOVE));
     }
 
+    @Override
     public boolean canUse() {
         if (!this.horse.isTamed() && this.horse.isVehicle()) {
-            Vec3 vec3 = DefaultRandomPos.getPos(this.horse, 5, 4);
-            if (vec3 == null) {
+            Vec3 vec3d = DefaultRandomPos.getPos(this.horse, 5, 4);
+
+            if (vec3d == null) {
                 return false;
             } else {
-                this.posX = vec3.x;
-                this.posY = vec3.y;
-                this.posZ = vec3.z;
+                this.posX = vec3d.x;
+                this.posY = vec3d.y;
+                this.posZ = vec3d.z;
                 return true;
             }
         } else {
@@ -36,17 +39,21 @@
         }
     }
 
+    @Override
     public void start() {
         this.horse.getNavigation().moveTo(this.posX, this.posY, this.posZ, this.speedModifier);
     }
 
+    @Override
     public boolean canContinueToUse() {
         return !this.horse.isTamed() && !this.horse.getNavigation().isDone() && this.horse.isVehicle();
     }
 
+    @Override
     public void tick() {
         if (!this.horse.isTamed() && this.horse.getRandom().nextInt(50) == 0) {
-            Entity entity = this.horse.getPassengers().get(0);
+            Entity entity = (Entity) this.horse.getPassengers().get(0);
+
             if (entity == null) {
                 return;
             }
@@ -54,8 +61,10 @@
             if (entity instanceof Player) {
                 int i = this.horse.getTemper();
                 int j = this.horse.getMaxTemper();
-                if (j > 0 && this.horse.getRandom().nextInt(j) < i) {
-                    this.horse.tameWithName((Player)entity);
+
+                // CraftBukkit - fire EntityTameEvent
+                if (j > 0 && this.horse.getRandom().nextInt(j) < i && !org.bukkit.craftbukkit.v1_17_R1.event.CraftEventFactory.callEntityTameEvent(this.horse, ((org.bukkit.craftbukkit.v1_17_R1.entity.CraftHumanEntity) this.horse.getBukkitEntity().getPassenger()).getHandle()).isCancelled()) {
+                    this.horse.tameWithName((Player) entity);
                     return;
                 }
 
@@ -64,7 +73,7 @@
 
             this.horse.ejectPassengers();
             this.horse.makeMad();
-            this.horse.level.broadcastEntityEvent(this.horse, (byte)6);
+            this.horse.level.broadcastEntityEvent(this.horse, (byte) 6);
         }
 
     }
